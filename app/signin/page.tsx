import PageWrapper from '@/components/PageWrapper';
import { DynamicEmbeddedWidget } from '@/lib/dynamicxyz';

export default function Signin() {
  return (
    <PageWrapper id="page-signin">
      <DynamicEmbeddedWidget background="default" />
    </PageWrapper>
  );
}
